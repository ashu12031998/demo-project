# Cloud Vendor API

Spring Boot project to manage cloud vendors.

## Features
- CRUD operations for vendors
- Exception handling
- REST API endpoints

## How to run
1. Clone the repo
2. Run `mvn spring-boot:run`
3. Access APIs at `http://localhost:8080`
